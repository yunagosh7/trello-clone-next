/** @type {import('next').NextConfig} */
const nextConfig = {
    // Allows import images from domains specified
    // images: {
    //     domains:['domain.com']
    // }
}

module.exports = nextConfig
